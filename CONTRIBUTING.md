# Contributing

感谢你改进 Loop Engineering。这个项目优先接受可复现、可验证、能帮助读者动手的
贡献。

## 可以贡献什么

- 修正错别字、失效链接或跨平台命令；
- 为现有章节补充最小可运行示例；
- 提交新的 false-DONE、奖励投机或停滞反例；
- 改进验证器、策略门、Reviewer 或评估协议；
- 补充 Linux、macOS 与不同 Python 版本的验证记录。

## 内容标准

新增内容应满足：

1. 明确区分代理声明、机械证据和人工判断；
2. 给出前置条件、执行命令、预期输出和恢复方式；
3. 失败状态使用具体名称，不只写 `success=false`；
4. 示例不得要求提交密码、Token、个人数据或生成日志；
5. 涉及破坏实验时，必须说明目标范围和恢复步骤；
6. 外部事实与工具行为应提供可复核来源或版本信息。

主课程内容还应保持“预测 → 运行 → 观察证据 → 解释状态 → 改一个变量 → 故障注入 → 独立复现”的节奏。
抽象概念必须至少对应一次可执行实验；新实验应给出预期终态和恢复命令。

## 开发流程

1. Fork 仓库并创建主题分支；
2. 保持一次 PR 只解决一个清晰问题；
3. 更新相关 Markdown 与示例；
4. 运行本地检查：

```powershell
python scripts/check_docs.py
python -m unittest discover -s tests -v
```

5. 在 PR 中写明动机、改动范围、验证命令和仍未覆盖的边界。

## 文档约定

- 面向中文读者写作，首次出现的关键英文术语可保留；
- 使用 ATX 标题（`#`、`##`），不要手写章节目录；
- 命令、文件名、退出码和终态使用行内代码；
- 多行代码必须注明语言；
- 优先使用相对链接，确保 Fork 后仍可导航；
- 图片放入 `docs/assets/chXX/`，文件名使用英文、数字和连字符。

## 原始 Word 讲义

`sources/word/` 只用于本地迁移，DOCX 文件不会纳入版本控制。请直接修改 Markdown；
只有在确实需要重新迁移时才运行 `scripts/docx_to_markdown.py`，并仔细审查生成差异。
